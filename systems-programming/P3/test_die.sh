#!/bin/sh
# test_die.sh - Die Command Test
# the 'die' command should print the message and terminate the shell immediately

echo Starting die test
die Fatal error encountered.
echo This should not print.
